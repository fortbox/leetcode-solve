package No476_数字的补数;

public class Solution {
    public int findComplement(int num){
        //1.先判断几位
        String s=Integer.toBinaryString(num);
        int n=s.length();
        //2.再求和
        for (int i = 0; i < n; i++) {



        }
    }
}
